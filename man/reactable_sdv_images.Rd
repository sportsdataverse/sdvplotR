% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reactable_sdv.R
\name{reactable_sdv_images}
\alias{reactable_sdv_images}
\alias{reactable_sdv_logos}
\alias{reactable_sdv_wordmarks}
\alias{reactable_sdv_headshots}
\title{Render Team Logos, Wordmarks and Headshots in 'reactable' Tables}
\usage{
reactable_sdv_logos(
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  variant = c("primary", "dark", "light", "alt", "classic", "helmet"),
  height = 30,
  default_img = NULL
)

reactable_sdv_wordmarks(
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  variant = c("primary", "dark", "light", "alt", "classic"),
  height = 30,
  default_img = NULL
)

reactable_sdv_headshots(
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  height = 40,
  default_img = NULL,
  id_type = NULL
)
}
\arguments{
\item{sport}{Character string identifying the sport. One of
\code{\link[=supported_sports]{supported_sports()}}.}

\item{variant}{Character. Logo variant: \code{"primary"}, \code{"dark"}, \code{"light"},
\code{"alt"}, \code{"classic"}, or \code{"helmet"} (NFL only). Falls back to the primary
image when the requested variant is not available for a team.}

\item{height}{Numeric. Image height in pixels.}

\item{default_img}{Character. Fallback image URL used when the value cannot
be resolved. If \code{NULL} (the default) the raw value is shown instead.}

\item{id_type}{Which ID system the player IDs hold: \code{NULL} (the default;
GSIS IDs for the NFL, ESPN athlete IDs otherwise), \code{"espn"} or \code{"league"}
(NBA / WNBA Stats \code{PERSON_ID}, MLBAM ID, GSIS). See \code{\link[=geom_sdv_headshots]{geom_sdv_headshots()}}.}
}
\value{
A function with signature \verb{function(value, index)} suitable for the
\code{cell} argument of \code{\link[reactable:colDef]{reactable::colDef()}}.
}
\description{
Cell renderers for \code{\link[reactable:colDef]{reactable::colDef()}} that translate team
abbreviations (or player IDs) into \verb{<img>} tags. Values that cannot be
resolved are returned unchanged so the original text is shown.
}
\examples{
\dontshow{if (requireNamespace("reactable", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(reactable)
library(sdvplotR)

df <- data.frame(team = c("KC", "BUF", "SF"), wins = c(13, 12, 11))

reactable(
  df,
  columns = list(
    team = colDef(cell = reactable_sdv_logos(sport = "nfl"), html = TRUE)
  )
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=reactable_sdv_cols_label]{reactable_sdv_cols_label()}}, \code{\link[=reactable_sdv_team_color_bar]{reactable_sdv_team_color_bar()}},
\code{\link[=gt_sdv_logos]{gt_sdv_logos()}}
}
