% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reactable_sdv.R
\name{reactable_sdv_cols_label}
\alias{reactable_sdv_cols_label}
\title{Replace 'reactable' Column Headers with Team Logos}
\usage{
reactable_sdv_cols_label(
  .data,
  ...,
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  variant = c("primary", "dark", "light", "alt", "classic", "helmet"),
  height = 30
)
}
\arguments{
\item{.data}{A data frame whose column names are team abbreviations.}

\item{...}{Additional arguments passed to every \code{\link[reactable:colDef]{reactable::colDef()}}.}

\item{sport}{Character string identifying the sport. One of
\code{\link[=supported_sports]{supported_sports()}}.}

\item{variant}{Character. Logo variant: \code{"primary"}, \code{"dark"}, \code{"light"},
\code{"alt"}, \code{"classic"}, or \code{"helmet"} (NFL only). Falls back to the primary
image when the requested variant is not available for a team.}

\item{height}{Numeric. Image height in pixels.}
}
\value{
A named list of \code{colDef} objects for the \code{columns} argument of
\code{\link[reactable:reactable]{reactable::reactable()}}.
}
\description{
Builds a named list of \code{\link[reactable:colDef]{reactable::colDef()}} objects whose
headers are team logos, for data frames whose column names are team
abbreviations. Columns that cannot be resolved are left out of the list so
\code{reactable} falls back to the plain column name.
}
\examples{
\dontshow{if (requireNamespace("reactable", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(reactable)
library(sdvplotR)

df <- data.frame(KC = 1:3, BUF = 4:6, SF = 7:9)
reactable(df, columns = reactable_sdv_cols_label(df, sport = "nfl"))
\dontshow{\}) # examplesIf}
}
