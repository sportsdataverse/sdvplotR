% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{theme_bg}
\alias{theme_bg}
\title{Background colors used by the \verb{gt_theme_*()} table themes}
\format{
A tibble with one row per theme, and one per style for the themes
that take a \code{style} argument:
\describe{
\item{theme}{The theme function name.}
\item{has_style}{The \code{style} value the row applies to (\code{"light"} or
\code{"dark"}) for \code{\link[=gt_theme_sdv]{gt_theme_sdv()}}, \code{\link[=gt_theme_sofa]{gt_theme_sofa()}} and \code{\link[=gt_theme_tier]{gt_theme_tier()}}, or \code{""} for themes
without one.}
\item{bg}{The background color the theme applies, as a hex code.}
}
}
\source{
Read back from each theme's \code{table.background.color} by
\code{data-raw/theme_bg.R}. \code{\link[=gt_theme_drench]{gt_theme_drench()}} takes its background from its
\code{color} argument and \code{\link[=gt_theme_broadsheet]{gt_theme_broadsheet()}} from its \code{paper} argument;
their rows hold the default.
}
\usage{
theme_bg
}
\description{
A lookup of the background color each \verb{gt_theme_*} function sets, used to match
a saved image's canvas to the table sitting on it. \code{\link[=gt_save_crop]{gt_save_crop()}} and
\code{\link[=gt_social_crop]{gt_social_crop()}} pad the image using their own \code{bg} argument, and a mismatch
shows up as a border around the table.
}
\examples{
# look up the background a theme uses, then match the canvas to it
bg <- theme_bg$bg[theme_bg$theme == "gt_theme_gtutils"]
bg

\dontshow{if (interactive() && requireNamespace("webshot2", quietly = TRUE) && isTRUE(file.exists(suppressMessages(chromote::find_chrome())))) withAutoprint(\{ # examplesIf}
# saving needs a headless Chrome (webshot2)
\donttest{
gt::gt(head(mtcars)) \%>\%
  gt_theme_gtutils() \%>\%
  gt_save_crop(tempfile(fileext = ".png"), bg = bg)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=gt_save_crop]{gt_save_crop()}}, \code{\link[=gt_social_crop]{gt_social_crop()}}.
}
\keyword{datasets}
