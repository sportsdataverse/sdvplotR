% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_sdv.R
\name{scale_axes_sdv}
\alias{scale_x_sdv}
\alias{scale_y_sdv}
\alias{scale_x_sdv_headshots}
\alias{scale_y_sdv_headshots}
\title{Axis Scales for Sports Team Logos}
\usage{
scale_x_sdv(
  ...,
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  expand = ggplot2::waiver(),
  guide = ggplot2::waiver(),
  position = "bottom",
  size = 12
)

scale_y_sdv(
  ...,
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  expand = ggplot2::waiver(),
  guide = ggplot2::waiver(),
  position = "left",
  size = 12
)

scale_x_sdv_headshots(
  ...,
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  expand = ggplot2::waiver(),
  guide = ggplot2::waiver(),
  position = "bottom",
  size = 20
)

scale_y_sdv_headshots(
  ...,
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  expand = ggplot2::waiver(),
  guide = ggplot2::waiver(),
  position = "left",
  size = 30
)
}
\arguments{
\item{...}{Other arguments passed to \code{\link[ggplot2:scale_x_discrete]{ggplot2::scale_x_discrete()}} or
\code{\link[ggplot2:scale_y_discrete]{ggplot2::scale_y_discrete()}}.}

\item{sport}{Character string identifying the sport.}

\item{expand}{Expansion limits for the axis.}

\item{guide}{Guide for the axis.}

\item{position}{Position of the axis.}

\item{size}{The logo size in pixels. It is applied as height for an x-scale
and as width for a y-scale.}
}
\value{
A ggplot2 scale object.
}
\description{
These scale functions replace axis labels with team logos or
player headshots. They work by modifying the axis text theme element to
render images.
}
\details{
The scale translates team names into raw image HTML and places the
HTML as axis labels. Because of the way ggplots are constructed, it is
necessary to adjust the theme after calling this scale. This can be done
by calling \code{\link[=theme_x_sdv]{theme_x_sdv()}} or \code{\link[=theme_y_sdv]{theme_y_sdv()}} or alternatively by manually
changing the relevant \code{axis.text} to \code{\link[ggtext:element_markdown]{ggtext::element_markdown()}}.
}
\examples{
\donttest{
library(sdvplotR)
library(ggplot2)

team_abbr <- valid_team_names("nfl")[1:8]

df <- data.frame(
  random_value = runif(length(team_abbr), 0, 1),
  teams = team_abbr
)

if (requireNamespace("ggtext", quietly = TRUE)) {
  ggplot(df, aes(x = teams, y = random_value)) +
    geom_col(aes(fill = teams), width = 0.5) +
    scale_fill_sdv(sport = "nfl", alpha = 0.4) +
    scale_x_sdv(sport = "nfl") +
    theme_minimal() +
    theme_x_sdv()
}
}
}
\seealso{
\code{\link[=theme_x_sdv]{theme_x_sdv()}}, \code{\link[=theme_y_sdv]{theme_y_sdv()}}
}
