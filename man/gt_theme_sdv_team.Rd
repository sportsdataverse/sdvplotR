% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_sdv.R
\name{gt_theme_sdv_team}
\alias{gt_theme_sdv_team}
\title{Team-colored SportsDataverse theme for \code{gt} tables}
\usage{
gt_theme_sdv_team(
  gt_object,
  team = NULL,
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  density = c("comfortable", "compact", "social"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{team}{Character. One team, as an abbreviation, alias or name for
\code{sport}. Defaults to \code{NULL}, which uses the SportsDataverse colors.}

\item{sport}{Character. The league \code{team} belongs to; see
\code{\link[=supported_sports]{supported_sports()}}. Defaults to \code{"nfl"}.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"comfortable"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
\code{\link[=gt_theme_sdv]{gt_theme_sdv()}} dressed in one team's colors: the title block is filled
with the team's primary color, the horizon line under the column labels takes
the secondary color, and the column labels are set in the primary color. The
colors come from \code{\link[=sdv_team_colors]{sdv_team_colors()}}, so any abbreviation, alias or name
\code{\link[=clean_team_abbrs]{clean_team_abbrs()}} resolves works here.
}
\details{
Text on the filled title block is black or white, whichever contrasts more
with the team's primary color, and the subtitle is blended toward it until it
still clears a 4.5:1 contrast ratio. When the secondary color would vanish
against the white table (a white or pale secondary), the horizon line uses the
primary color instead, and column labels fall back to the SportsDataverse
navy when the primary color is too light to read on white. With
\code{team = NULL} the table wears the SportsDataverse navy and cyan.
}
\section{Figures}{

\if{html}{\figure{gt_theme_sdv_team.png}{options: width=100\%}}
}

\section{Density}{


\code{density} scales the theme's type and row padding together. \code{"comfortable"}
leaves every size as the theme sets it, \code{"compact"} scales both down, and
\code{"social"} scales both up, to the scale \code{\link[=gt_save_crop]{gt_save_crop()}} and
\code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\examples{
library(gt)
leaders <- data.frame(
  player = c("Patrick Mahomes", "Travis Kelce", "Isiah Pacheco"),
  yards = c(4183, 984, 935)
)
gt(leaders) |>
  tab_header("Chiefs yardage leaders", "2023 regular season") |>
  gt_theme_sdv_team(team = "KC", sport = "nfl")

}
\seealso{
\code{\link[=gt_theme_sdv]{gt_theme_sdv()}}, \code{\link[=sdv_team_colors]{sdv_team_colors()}}.
}
