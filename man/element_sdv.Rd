% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_elements.R
\name{element_sdv_logo}
\alias{element_sdv_logo}
\alias{element_sdv_wordmark}
\alias{element_sdv_headshot}
\alias{element_sdv_raster}
\title{Theme Elements for Image Grobs}
\usage{
element_sdv_logo(
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  alpha = NULL,
  colour = NA,
  color = NULL,
  hjust = NULL,
  vjust = NULL,
  size = 0.5
)

element_sdv_wordmark(
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  alpha = NULL,
  colour = NA,
  color = NULL,
  hjust = NULL,
  vjust = NULL,
  size = 0.5
)

element_sdv_headshot(
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  alpha = NULL,
  colour = NA,
  color = NULL,
  hjust = NULL,
  vjust = NULL,
  size = 0.5
)

element_sdv_raster(
  image_path,
  x = grid::unit(0.5, "npc"),
  y = grid::unit(0.5, "npc"),
  width = grid::unit(1, "npc"),
  height = grid::unit(1, "npc"),
  just = "centre",
  hjust = 0.5,
  vjust = 0.5,
  interpolate = TRUE,
  ...
)
}
\arguments{
\item{sport}{Character string identifying the sport. One of
\code{\link[=supported_sports]{supported_sports()}}.}

\item{alpha}{The alpha channel (transparency) between 0 and 1.}

\item{colour, color}{The image will be colorized with this color. Use \code{"b/w"}
for black and white.}

\item{hjust}{Horizontal justification.}

\item{vjust}{Vertical justification.}

\item{size}{The output grob size in \code{cm}.}

\item{image_path}{A file path or url to an image.}

\item{x, y, width, height, just, interpolate}{Passed on to
\code{\link[ggpath:element_raster]{ggpath::element_raster()}}.}

\item{...}{Other arguments passed on to \code{\link[ggpath:element_raster]{ggpath::element_raster()}}.}
}
\value{
An S3 object of class \code{element}.
}
\description{
In conjunction with the \link[ggplot2:theme]{ggplot2::theme} system, the following \code{element_}
functions enable images in non-data components of the plot, e.g. axis text.
\itemize{
\item \code{element_sdv_logo()}: draws team logos instead of their abbreviations.
\item \code{element_sdv_wordmark()}: draws team wordmarks instead of their abbreviations.
\item \code{element_sdv_headshot()}: draws player headshots instead of their IDs.
\item \code{element_sdv_raster()}: draws a single image in a rectangular theme
element such as \code{plot.background}. A thin wrapper around
\code{\link[ggpath:element_raster]{ggpath::element_raster()}}.
}
}
\details{
The elements translate team abbreviations or player IDs into
logo images or player headshots for the specified sport. Rendering is
delegated to \code{\link[ggpath:element_path]{ggpath::element_path()}}.
}
\examples{
\donttest{
library(sdvplotR)
library(ggplot2)

team_abbr <- valid_team_names("nfl")[1:16]

df <- data.frame(
  random_value = runif(length(team_abbr), 0, 1),
  teams = team_abbr
)

# use logos for x-axis
ggplot(df, aes(x = teams, y = random_value)) +
  geom_col(aes(color = teams, fill = teams), width = 0.5) +
  scale_color_sdv(sport = "nfl", type = "secondary") +
  scale_fill_sdv(sport = "nfl", alpha = 0.4) +
  theme_minimal() +
  theme(axis.text.x = element_sdv_logo(sport = "nfl"))
}
}
\seealso{
\code{\link[ggpath:element_path]{ggpath::element_path()}}, \code{\link[ggpath:element_raster]{ggpath::element_raster()}}
}
