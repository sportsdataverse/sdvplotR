% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gt_theme_sdv.R
\name{gt_theme_sdv}
\alias{gt_theme_sdv}
\title{SportsDataverse theme for \code{gt} tables}
\usage{
gt_theme_sdv(
  gt_object,
  style = c("light", "dark"),
  density = c("comfortable", "compact", "social"),
  ...
)
}
\arguments{
\item{gt_object}{A \code{gt} table object to modify.}

\item{style}{Character. \code{"light"} for a white table, or \code{"dark"} for the
SportsDataverse navy. Defaults to \code{"light"}.}

\item{density}{Character. The type and padding scale. One of \code{"comfortable"},
\code{"compact"}, or \code{"social"}. See Density. Defaults to \code{"comfortable"}.}

\item{...}{Additional arguments passed to \code{gt::tab_options}, applied last so
they override anything the theme sets.}
}
\value{
Returns a modified \code{gt} table with the theme applied.
}
\description{
The SportsDataverse house table: Chivo titles and column labels, a Lato body
set in tabular figures, faint row dividers, and the SDV gradient
(\verb{#3346F0} to \verb{#7FE6DC}) drawn as a single horizon line under the column
labels. \code{style = "dark"} sets the same table on the SportsDataverse navy,
matching the dark mode of the package websites.
}
\details{
The gradient line is the theme's one accent; everything else stays quiet so
logos from \code{\link[=gt_sdv_logos]{gt_sdv_logos()}} and team colors carry the table. Titles and the
heading are left-aligned. Column labels keep the case you give them rather
than being forced to capitals. For a table dressed in one team's colors, see
\code{\link[=gt_theme_sdv_team]{gt_theme_sdv_team()}}.
}
\section{Density}{


\code{density} scales the theme's type and row padding together. \code{"comfortable"}
leaves every size as the theme sets it, \code{"compact"} scales both down, and
\code{"social"} scales both up, to the scale \code{\link[=gt_save_crop]{gt_save_crop()}} and
\code{\link[=gt_social_crop]{gt_social_crop()}} export at.
}

\section{Figures}{

\if{html}{\figure{gt_theme_sdv.png}{options: width=100\%}}

\if{html}{\figure{gt_theme_sdv_dark.png}{options: width=100\%}}
}

\examples{
library(gt)
standings <- data.frame(
  team = c("KC", "LAC", "DEN", "LV"),
  w = c(15, 11, 10, 4), l = c(2, 6, 7, 13)
)
gt(standings) |>
  gt_sdv_logos(columns = "team", sport = "nfl") |>
  tab_header("AFC West standings", "Through week 18") |>
  gt_theme_sdv()

gt(standings) |> gt_theme_sdv(style = "dark", density = "social")

}
\seealso{
\code{\link[=gt_theme_sdv_team]{gt_theme_sdv_team()}}, \link{theme_bg} for the background to pad a saved
image with.
}
