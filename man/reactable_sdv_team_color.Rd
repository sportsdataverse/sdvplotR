% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reactable_sdv.R
\name{reactable_sdv_team_color}
\alias{reactable_sdv_team_color}
\alias{reactable_sdv_team_color_bar}
\alias{reactable_sdv_team_color_bg}
\title{Team-Colored Cell Styles for 'reactable' Tables}
\usage{
reactable_sdv_team_color_bar(
  data,
  team_col,
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  type = c("primary", "secondary"),
  max_value = NULL,
  na_color = "grey70"
)

reactable_sdv_team_color_bg(
  data,
  team_col,
  sport = c("nfl", "nba", "wnba", "mlb", "nhl", "cfb", "mbb", "wbb"),
  type = c("primary", "secondary"),
  alpha = 0.15,
  na_color = "grey70"
)
}
\arguments{
\item{data}{The data frame passed to \code{\link[reactable:reactable]{reactable::reactable()}}. Needed to
look up the team of each row.}

\item{team_col}{Character. Name of the column in \code{data} holding team
abbreviations.}

\item{sport}{Character string identifying the sport. One of
\code{\link[=supported_sports]{supported_sports()}}.}

\item{type}{Character. \code{"primary"} or \code{"secondary"} team color.}

\item{max_value}{Numeric. Reference value that fills the whole cell. If
\code{NULL} (the default), the maximum of the styled column is used.}

\item{na_color}{Color used when a team cannot be resolved.}

\item{alpha}{Numeric. Opacity of the background fill in \verb{[0, 1]}.}
}
\value{
A function with signature \verb{function(value, index, name)} suitable
for the \code{style} argument of \code{\link[reactable:colDef]{reactable::colDef()}}.
}
\description{
Style functions for \code{\link[reactable:colDef]{reactable::colDef()}} that color cells
with a team's primary or secondary color: a horizontal bar whose length
is proportional to the cell value, or a translucent background fill.
}
\examples{
\dontshow{if (requireNamespace("reactable", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(reactable)
library(sdvplotR)

df <- data.frame(team = c("KC", "BUF", "SF"), wins = c(13, 12, 11))

reactable(
  df,
  columns = list(
    team = colDef(style = reactable_sdv_team_color_bg(df, "team", sport = "nfl")),
    wins = colDef(style = reactable_sdv_team_color_bar(df, "team", sport = "nfl"))
  )
)
\dontshow{\}) # examplesIf}
}
